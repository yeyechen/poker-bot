[pytest]
# Core config
testpaths = tests
python_files = test_*.py

# Default flags: verbose, show variables on failure, summary at end
addopts = -v --showlocals -ra --tb=short

# Custom markers (add these back only if you actually use @pytest.mark.slow etc.)
markers =
    unit
    integration
    slow
